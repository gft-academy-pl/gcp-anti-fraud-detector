const assert = require('assert');
const {
	google
} = require('googleapis');
const path = require('path');

describe('Sending e-mail', function() {

	it('should send it via API', async function() {
		
	})

});